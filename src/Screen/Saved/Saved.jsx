import styled from 'styled-components';

const Saved = () => {
  console.log('Saved');

  return (
    <Wrapper>
      <h2>Saved</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Saved;
