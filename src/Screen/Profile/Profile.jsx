import styled from 'styled-components';

const Profile = () => {
  console.log('Profile');

  return (
    <Wrapper>
      <h2>Profile</h2>
    </Wrapper>
  );
};

const Wrapper = styled.main`
  padding: 5px 5px;
`;

export default Profile;
